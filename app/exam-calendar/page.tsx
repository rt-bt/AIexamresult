import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Exam Calendar | All India Exam Result",
  description: "Track important dates for government exams — application start, last date, exam date, admit card, result & answer key for SSC, Railway, Banking & more.",
};

export { default } from "./page-client";
